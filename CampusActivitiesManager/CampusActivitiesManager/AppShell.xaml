<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CampusActivitiesManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:CampusActivitiesManager.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="Campus Activities Manager">

    <!-- 1. Màn hình Quản trị Tài khoản Admin (US 05) - MÀN HÌNH MẶC ĐỊNH MỞ ĐẦU -->
    <ShellContent
        Title="Quản trị Tài khoản (Admin)"
        Icon="{StaticResource IconAccounts}"
        ContentTemplate="{DataTemplate pages:AccountListPage}"
        Route="accounts" />

    <!-- 2. Màn hình Cổng Sinh Viên (Student Dashboard) -->
    <ShellContent
        Title="Cổng Sinh Viên"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:MainPage}"
        Route="main" />

    <!-- 3. Màn hình Đăng nhập (Login) -->
    <ShellContent
        Title="Đăng nhập / Chuyển tài khoản"
        Icon="{StaticResource IconPerson}"
        ContentTemplate="{DataTemplate pages:LoginPage}"
        Route="login" />

    <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
