<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CampusActivitiesManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:CampusActivitiesManager.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="CampusActivitiesManager">

    <Shell.FlyoutHeader>
        <Grid BackgroundColor="{StaticResource Primary}" Padding="20,30,20,20">
            <VerticalStackLayout Spacing="6">
                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Border WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 21" BackgroundColor="{StaticResource White}">
                        <Label Text="🎓" FontSize="22" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    <VerticalStackLayout VerticalOptions="Center" Spacing="1">
                        <Label Text="Campus Activities" TextColor="{StaticResource White}" FontFamily="SegoeSemibold" FontSize="16" />
                        <Label Text="Quản lý hoạt động" TextColor="{StaticResource Secondary}" FontSize="12" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <ShellContent
        Title="Dashboard"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:MainPage}"
        Route="main" />

    <ShellContent
        Title="Projects"
        Icon="{StaticResource IconProjects}"
        ContentTemplate="{DataTemplate pages:ProjectListPage}"
        Route="projects" />

    <ShellContent
        Title="Phân quyền (RBAC)"
        Icon="{StaticResource IconUserManagement}"
        ContentTemplate="{DataTemplate pages:UserManagementPage}"
        Route="users" />

    <ShellContent
        Title="Manage Meta"
        Icon="{StaticResource IconMeta}"
        ContentTemplate="{DataTemplate pages:ManageMetaPage}"
        Route="manage" />

    <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
